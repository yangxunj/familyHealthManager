import { Controller } from '@nestjs/common';

// Authentication is now handled by Supabase
// This controller is kept for potential future auth-related endpoints
@Controller('auth')
export class AuthController {}
